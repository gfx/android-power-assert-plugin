buildscript {
    repositories {
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:0.10.+'
        classpath 'com.jakewharton.sdkmanager:gradle-plugin:+'
    }
}

allprojects {
    repositories {
        mavenCentral()
    }
}

task('uploadArchives') {
}
