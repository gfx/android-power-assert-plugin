box: wercker/android
build:
  steps:
    - script:
        name: show base information
        code: |
          echo $ANDROID_HOME
          java  -version
          javac -version
    - script:
        name: run gradle
        code: |
            ./gradlew -q --full-stacktrace --project-cache-dir=$WERCKER_CACHE_DIR build
